<resources>

    <!-- Base application theme. -->
    <style name="AppTheme" parent="Theme.AppCompat.Light.NoActionBar">
        <!-- Customize your theme here. -->
    </style>

    <!-- dialog样式 -->
    <style name="customdialog_style" parent="@android:style/Theme.Dialog">
        <item name="android:windowFrame">@null</item>
        <item name="android:windowIsFloating">true</item>
        <item name="android:windowIsTranslucent">false</item>
        <item name="android:windowNoTitle">true</item>
        <item name="android:backgroundDimEnabled">true</item>
        <item name="android:background">@android:color/transparent</item>
        <item name="android:windowBackground">@android:color/transparent</item>
        <item name="android:windowAnimationStyle">@style/base_dialog_anim_style</item>
    </style>

    <!-- dialog anim -->
    <style name="base_dialog_anim_style">
        <item name="@android:windowEnterAnimation">@anim/base_dialog_enter</item>
        <item name="@android:windowExitAnimation">@anim/base_dialog_exit</item>
    </style>

</resources>
